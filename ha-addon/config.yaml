name: "f12mqtt"
description: "F1 Live Timing → MQTT Bridge for Home Assistant and AWTRIX 3"
version: "0.1.0"
slug: "f12mqtt"
url: "https://github.com/f12mqtt/f12mqtt"
arch:
  - aarch64
  - amd64
startup: application
boot: auto
ingress: true
ingress_port: 3000
panel_icon: "mdi:flag-checkered"
panel_title: "F1 MQTT"
map:
  - type: data
    read_only: false
options:
  port: 3000
schema:
  port: int
services:
  - mqtt:need
environment:
  DATA_DIR: /data
  RECORDINGS_DIR: /data/recordings
  DB_PATH: /data/config.db
